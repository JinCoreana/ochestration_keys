; test_minimal.ahk
; Minimal test that shows a popup immediately without hotkeys or a GUI setup.
; If this does not appear, AutoHotkey execution itself may be blocked on this PC
; by a security policy or endpoint protection tool.
#Requires AutoHotkey v2.0
MsgBox("AHK is running correctly.")